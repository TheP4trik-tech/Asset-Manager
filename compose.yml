services:
  web:
    build:
      dockerfile: Dockerfile
    volumes:
      - .:/rails
    command: bash -c "bin/rails server -b 0.0.0.0 -p 3000"
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development

